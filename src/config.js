
export const breakpoints = {
  sm: '(min-width: 32em)',
  md: '(min-width: 48em)',
  lg: '(min-width: 64em)'
}

export const scale = [
  0,
  8,
  16,
  32,
  64
]

const config = {
  breakpoints,
  scale
}

export default config

